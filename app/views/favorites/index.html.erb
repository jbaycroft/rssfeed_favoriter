<p id="notice"><%= notice %></p>

<h1>My Favorites</h1>
<ul class="list-group">
  <% @favorites.each do |favorite| %>
    <li class="list-group-item"><%= link_to favorite.title, favorite.url %></li>
  <% end %>
</ul>
